# 🔧 إصلاح مشكلة متغيرات البيئة

## المشكلة
```
Uncaught Error
Missing Supabase environment variables. Please check your .env file.
Required: EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY
```

## السبب
Expo لا يقرأ ملف `.env` بعد التحديث. يحتاج إلى إعادة تشغيل مع تنظيف الكاش.

---

## ✅ الحل السريع (اختر واحدة)

### الطريقة 1: إعادة تشغيل Expo مع تنظيف الكاش
```bash
# أوقف التطبيق الحالي (Ctrl+C)

# نظّف الكاش وأعد التشغيل
npx expo start --clear
```

---

### الطريقة 2: حذف الكاش يدوياً
```bash
# أوقف التطبيق الحالي (Ctrl+C)

# احذف مجلدات الكاش
rm -rf node_modules/.cache
rm -rf .expo

# أعد تشغيل التطبيق
npx expo start
```

---

### الطريقة 3: إعادة تثبيت Dependencies
```bash
# أوقف التطبيق الحالي (Ctrl+C)

# احذف node_modules
rm -rf node_modules

# أعد التثبيت
npm install

# أعد التشغيل
npx expo start --clear
```

---

## 🔍 التحقق من الحل

بعد إعادة التشغيل، يجب أن يعمل التطبيق بدون أخطاء.

إذا استمرت المشكلة، تأكد من:

### 1. ملف .env موجود في المجلد الرئيسي
```bash
ls -la .env
```

### 2. المتغيرات موجودة في .env
```bash
cat .env
```

يجب أن تشاهد:
```
EXPO_PUBLIC_SUPABASE_URL=https://gpucisjxecupcyosumgy.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 3. تأكد من تثبيت dotenv
```bash
npm list dotenv
```

إذا لم يكن مثبتاً:
```bash
npm install dotenv
```

---

## 📱 ملاحظات مهمة

### لماذا EXPO_PUBLIC_؟
- في Expo، المتغيرات التي تبدأ بـ `EXPO_PUBLIC_` يمكن الوصول إليها من الكود
- المتغيرات بدون `EXPO_PUBLIC_` لا يمكن الوصول إليها في React Native

### متى تحتاج إعادة التشغيل؟
- عند تغيير ملف `.env`
- عند إضافة متغيرات جديدة
- عند تحديث قيم المتغيرات

### الكاش في Expo
- Expo يحتفظ بكاش للمتغيرات
- يجب تنظيف الكاش بعد تغيير `.env`
- استخدم `--clear` دائماً بعد تغيير `.env`

---

## 🚀 الأمر الموصى به

```bash
# الأمر الكامل الموصى به
npx expo start --clear --reset-cache
```

هذا الأمر:
- ✅ ينظف كاش Expo
- ✅ يعيد تعيين Metro bundler cache
- ✅ يقرأ ملف `.env` من جديد

---

## ❓ الأسئلة الشائعة

### س: هل يجب إعادة التشغيل في كل مرة؟
ج: فقط عند تغيير ملف `.env`

### س: هل يمكن استخدام .env.local؟
ج: نعم، لكن تأكد من إضافته إلى `.gitignore`

### س: هل المتغيرات آمنة؟
ج: المتغيرات التي تبدأ بـ `EXPO_PUBLIC_` تُضمّن في التطبيق المبني، لذا لا تضع مفاتيح سرية فيها. استخدم فقط المفاتيح العامة (Public Keys).

---

## 📞 الدعم

إذا استمرت المشكلة:
- تحقق من نسخة Expo: `npx expo --version`
- تحقق من نسخة Node: `node --version`
- راجع logs: `npx expo start --clear --verbose`

---

**تاريخ الإنشاء:** 27 ديسمبر 2024  
**الحالة:** جاهز للتطبيق
